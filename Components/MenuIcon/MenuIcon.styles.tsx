import React from "react";
import { StyleSheet } from "react-native";

const menu_icon = StyleSheet.create({
    wrapper: {
        width: 22,
        height: 22,
    },

    img_background: {
        flex: 1,
        width: null,
        height: null,
        resizeMode: 'contain',
    }
})

export default menu_icon