import React from "react";
import { StyleSheet } from "react-native";

const choose_rareness_labels_styles = StyleSheet.create({
    wrapper:{
        flex: 1,
        flexDirection: "row",
        justifyContent: "space-evenly",
    },

    image_wrapper: {
        height: "100%",
        width: "17%",
    }
})

export default choose_rareness_labels_styles