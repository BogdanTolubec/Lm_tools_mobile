import React from "react"
import { StyleSheet } from "react-native"

const jewel_component_styles = StyleSheet.create({
    wrapper: {
        height: "100%",
        width: "100%",
        padding: 1
    },
})

export default jewel_component_styles