import React from "react";
import { StyleSheet } from "react-native";

const items_carousel_styles = StyleSheet.create({
    wrapper: {
        height: "100%",
        width: "100%",
        overflow: "hidden",
    },

    slide: {
        alignItems: "center",
        justifyContent: "center",
    },
})

export default items_carousel_styles