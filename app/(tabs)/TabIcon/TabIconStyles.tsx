import React from "react";
import { StyleSheet } from "react-native";

const tab_icon = StyleSheet.create({
    wrapper: {
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        gap: 2,
    },

    img: {
        width: 40,
        height: 40,
    }
}
)

export default tab_icon